import { createContext } from "react";

// you can use multiple global file
// firstly you have to creat the context which gives you a "object"
const GlobalContext =  createContext("Rohit");

export default GlobalContext;
// this is your global file firstly you have to create this file so everyone can use
