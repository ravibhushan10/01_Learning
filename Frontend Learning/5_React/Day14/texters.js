// This is global state : to access use "state.slice_name.state_name"
const state = {
    slice1:{
        count:0
    },
    slice2:{
        count:2,
        name:"Rohit"
    },
    slice3:{
        login:true,
    }
}
